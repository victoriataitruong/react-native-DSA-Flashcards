A binary search tree (BST) is a special kind of binary tree. In a BST:
•	The numbers in the left branch are smaller than the parent number.
•	The numbers in the right branch are larger than the parent number.